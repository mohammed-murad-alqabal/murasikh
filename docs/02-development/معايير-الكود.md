# 📏 معايير الكود

معايير البرمجة المتبعة في مشروع مُرَسِّخ لضمان جودة وسهولة صيانة الكود.

---

## 🐍 Python (Backend)

### التنسيق العام

```python
# ✅ صحيح
def get_recommendation(user_id: int, text: str) -> Recommendation:
    """
    الحصول على توصية للمستخدم.
    
    Args:
        user_id: معرف المستخدم
        text: النص المدخل
        
    Returns:
        كائن التوصية
    """
    # الكود هنا
    pass


# ❌ خطأ
def get_recommendation(user_id,text):
    # الكود هنا
    pass
```

### الأدوات المستخدمة

| الأداة | الغرض | ملف التكوين |
|--------|-------|-------------|
| **Black** | تنسيق الكود | `pyproject.toml` |
| **isort** | ترتيب الاستيرادات | `pyproject.toml` |
| **flake8** | فحص الجودة | `.flake8` |
| **mypy** | فحص الأنواع | `mypy.ini` |
| **pytest** | الاختبارات | `pytest.ini` |

### قواعد التسمية

```python
# المتغيرات والدوال: snake_case
user_name = "Ahmed"
def get_user_by_id(user_id: int) -> User:
    pass

# الثوابت: UPPER_SNAKE_CASE
MAX_RETRIES = 3
DEFAULT_TIMEOUT = 30

# الفئات: PascalCase
class RecommendationService:
    pass

# المتغيرات الخاصة: _leading_underscore
_internal_cache = {}

# المتغيرات المحمية: __leading_underscore
__database_connection = None
```

### الاستيرادات

```python
# ✅ الترتيب الصحيح (isort)

# 1. مكتبة Python القياسية
import json
from typing import Dict, List, Optional

# 2. مكتبات الطرف الثالث
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel
import redis

# 3. وحدات المشروع
from app.core.config import settings
from app.db.models import User, Interaction
from app.services.ai import EmbeddingService
```

### Type Hints

```python
# ✅ استخدام Type Hints دائماً
from typing import Dict, List, Optional

def search_similar(
    query: str,
    top_k: int = 5,
    filters: Optional[Dict] = None
) -> List[Dict]:
    pass

# استخدام Union للأنواع المتعددة
from typing import Union
def process_data(data: Union[str, bytes]) -> str:
    pass

# استخدام Literal للقيم المحددة
from typing import Literal
def set_tier(tier: Literal["minimal", "moderate", "full"]) -> None:
    pass
```

### Docstrings

```python
def create_embedding(text: str) -> list:
    """
    تحويل نص إلى متجه باستخدام GATE-AraBERT.
    
    Args:
        text: النص العربي المراد تحويله
        
    Returns:
        قائمة من الأرقام تمثل المتجه
        
    Raises:
        ValueError: إذا كان النص فارغاً
        
    Example:
        >>> create_embedding("أشعر بالسعادة")
        [0.123, -0.456, ...]
    """
    if not text:
        raise ValueError("النص لا يمكن أن يكون فارغاً")
    
    return model.encode([text]).tolist()[0]
```

### معالجة الأخطاء

```python
# ✅ معالجة محددة
try:
    result = await api_call()
except ConnectionError as e:
    logger.error(f"فشل الاتصال: {e}")
    raise HTTPException(status_code=503, detail="الخدمة غير متاحة")
except ValueError as e:
    logger.warning(f"قيمة غير صالحة: {e}")
    raise HTTPException(status_code=400, detail=str(e))


# ❌ معالجة عامة (تجنبها)
try:
    result = await api_call()
except Exception:
    pass  # لا تفعل هذا!
```

---

## 📱 Dart/Flutter (Frontend)

### التنسيق العام

```dart
// ✅ صحيح
class RecommendationCard extends StatelessWidget {
  const RecommendationCard({
    super.key,
    required this.recommendation,
  });

  final Recommendation recommendation;

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Padding(
        padding: const EdgeInsets.all(16.0),
        child: Text(recommendation.arabicText),
      ),
    );
  }
}
```

### قواعد التسمية

```dart
// المتغيرات والدوال: camelCase
String userName = 'Ahmed';
void getUserById(int userId) {}

// الثوابت: lowerCamelCase
const int maxRetries = 3;
const String defaultLanguage = 'ar';

// الفئات: PascalCase
class RecommendationService {}

// المتغيرات الخاصة: _leadingCamelCase
class _InternalState {
  String _cachedValue = '';
}
```

### State Management (BLoC)

```dart
// events.dart
abstract class RecommendationEvent extends Equatable {
  const RecommendationEvent();
  
  @override
  List<Object> get props => [];
}

class GetRecommendation extends RecommendationEvent {
  final String text;
  final String? emotionHint;
  
  const GetRecommendation({
    required this.text,
    this.emotionHint,
  });
  
  @override
  List<Object> get props => [text];
}

// states.dart
abstract class RecommendationState extends Equatable {
  const RecommendationState();
  
  @override
  List<Object?> get props => [];
}

class RecommendationLoading extends RecommendationState {}

class RecommendationLoaded extends RecommendationState {
  final Recommendation recommendation;
  
  const RecommendationLoaded(this.recommendation);
  
  @override
  List<Object?> get props => [recommendation];
}
```

### Widgets

```dart
// ✅ تقسيم الـ Widgets الكبيرة
class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: _buildAppBar(),
      body: _buildBody(context),
    );
  }
  
  PreferredSizeWidget _buildAppBar() {
    return AppBar(title: const Text('مُرَسِّخ'));
  }
  
  Widget _buildBody(BuildContext context) {
    return Column(
      children: [
        _buildInputField(),
        _buildEmotionSelector(),
        _buildRecommendationButton(),
      ],
    );
  }
  
  // ... methods
}
```

---

## 🗄️ قاعدة البيانات

### SQL

```sql
-- ✅ أسماء الجداول: snake_case جميز
CREATE TABLE user_interactions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    is_deleted BOOLEAN DEFAULT FALSE
);

-- الفهارس
CREATE INDEX idx_user_interactions_user_id ON user_interactions(user_id);
CREATE INDEX idx_user_interactions_created_at ON user_interactions(created_at);
```

### SQLAlchemy Models

```python
from sqlalchemy import Column, Integer, String, Text, Boolean, TIMESTAMP
from sqlalchemy.sql import func
from app.db.base import Base

class UserInteraction(Base):
    __tablename__ = "user_interactions"
    
    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, nullable=False, index=True)
    query_text = Column(Text)
    created_at = Column(
        TIMESTAMP(timezone=True),
        server_default=func.now(),
        nullable=False
    )
    is_deleted = Column(Boolean, default=False)
    
    def __repr__(self) -> str:
        return f"<UserInteraction(id={self.id}, user_id={self.user_id})>"
```

---

## 📝 التوثيق

### التعليقات

```python
# ✅ تعليق يشرح "لماذا" وليس "ماذا"
# استخدمنا خوارزمية BM25 للبحث لأنها أفضل للنصوص العربية
# من الخوارزميات القياسية
def search_arabic_text(query: str) -> List[str]:
    pass

# ❌ تعليق غير مفيد
# البحث في النصوص العربية
def search_arabic_text(query: str) -> List[str]:
    pass
```

---

## ✅ التحقق من الجودة

### Python

```bash
# تنسيق الكود
black app/
isort app/

# فحص الجودة
flake8 app/

# فحص الأنواع
mypy app/

# الاختبارات
pytest --cov=app tests/
```

### Flutter

```bash
# تنسيق الكود
dart format lib/

# تحليل الكود
dart analyze lib/

# الاختبارات
flutter test --coverage
```

---

## 🚫 أخطاء شائعة يجب تجنبها

### 1. Hard-coded Values

```python
# ❌ خطأ
api_url = "https://api.example.com/v1/recommend"

# ✅ صحيح
from app.core.config import settings
api_url = settings.API_URL
```

### 2. عدم معالجة الأخطاء

```python
# ❌ خطأ
response = requests.get(url)
data = response.json()

# ✅ صحيح
try:
    response = requests.get(url, timeout=30)
    response.raise_for_status()
    data = response.json()
except requests.RequestException as e:
    logger.error(f"فشل الطلب: {e}")
    raise
```

### 3. كود مكرر

```python
# ❌ خطأ - كود مكرر
def get_verse_by_id(verse_id: int):
    return db.query(Verse).filter(Verse.id == verse_id).first()

def get_verse_by_id(verse_id: int):
    return db.query(Verse).filter(Verse.id == verse_id).first()

# ✅ صحيح - كود موحد
def get_by_id(model: Type[Base], item_id: int):
    return db.query(model).filter(model.id == item_id).first()
```

---

## 📚 المراجع

- [PEP 8 - Style Guide for Python Code](https://peps.python.org/pep-0008/)
- [Effective Dart](https://dart.dev/guides/language/effective-dart)
- [Flutter Style Guide](https://github.com/flutter/flutter/wiki/Style-guide-for-Flutter-repo)

---

**آخر تحديث:** 2026-09-11
