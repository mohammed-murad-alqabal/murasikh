# 🔒 الأمان

قائمة التحقق الأمني لمشروع مُرَسِّخ.

---

## 📋 قائمة التحقق

### المصادقة والتفويض

| البند | الحالة | الملاحظات |
|-------|--------|-----------|
| كلمات المرور مشفرة (bcrypt) | ✅ | 12 rounds |
| JWT مُوقّع | ✅ | HS256 |
| Token expiration | ✅ | 30 دقيقة |
| Refresh token rotation | ✅ | - |
| Rate limiting | ✅ | 500 req/hour |

### الاتصال

| البند | الحالة | الملاحظات |
|-------|--------|-----------|
| HTTPS إجباري | ✅ | TLS 1.2+ |
| HSTS header | ✅ | max-age=31536000 |
| CORS مُحدد | ✅ | domains فقط |
| CSP header | ✅ | - |
| X-Frame-Options | ✅ | DENY |

### قاعدة البيانات

| البند | الحالة | الملاحظات |
|-------|--------|-----------|
| اتصال مشفر | ✅ | SSL required |
| مستخدم محدود الصلاحيات | ✅ | لا root |
| حقن SQL محمي | ✅ | ORM فقط |
| Backups مشفرة | ✅ | AES-256 |

### البيانات

| البند | الحالة | الملاحظات |
|-------|--------|-----------|
| البيانات الحساسة مشفرة | ✅ | AES-256 |
| Logs لا تحتوي secrets | ✅ | - |
| Input validation | ✅ | Pydantic |
| Output encoding | ✅ | - |

### API

| البند | الحالة | الملاحظات |
|-------|--------|-----------|
| API Keys محمية | ✅ | Env vars |
| Rate limiting | ✅ | Per user |
| Request validation | ✅ | Pydantic |
| Error messages آمنة | ✅ | لا تفاصيل داخلية |

---

## 🔐 التشفير

### كلمات المرور

```python
from passlib.context import CryptContext

pwd_context = CryptContext(
    schemes=["bcrypt"],
    deprecated="auto",
    bcrypt__rounds=12
)

# Hash
hashed = pwd_context.hash(plain_password)

# Verify
pwd_context.verify(plain_password, hashed)
```

### JWT

```python
from jose import jwt
from datetime import datetime, timedelta

def create_access_token(data: dict):
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes=30)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm="HS256")
```

### البيانات الحساسة

```python
from cryptography.fernet import Fernet

class DataEncryption:
    def __init__(self, key: bytes):
        self.fernet = Fernet(key)
    
    def encrypt(self, data: str) -> str:
        return self.fernet.encrypt(data.encode()).decode()
    
    def decrypt(self, encrypted: str) -> str:
        return self.fernet.decrypt(encrypted.encode()).decode()
```

---

## 🛡️ Headers الأمنية

```python
# app/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI()

# CORS
app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://murassikh.com"],
    allow_credentials=True,
    allow_methods=["GET", "POST"],
    allow_headers=["*"],
)

# Security Headers
@app.middleware("http")
async def add_security_headers(request, call_next):
    response = await call_next(request)
    response.headers["X-Content-Type-Options"] = "nosniff"
    response.headers["X-Frame-Options"] = "DENY"
    response.headers["X-XSS-Protection"] = "1; mode=block"
    response.headers["Strict-Transport-Security"] = "max-age=31536000; includeSubDomains"
    return response
```

---

## 🚨 Common Vulnerabilities

### SQL Injection

```python
# ❌ خطأ
query = f"SELECT * FROM users WHERE email = '{email}'"

# ✅ صحيح
from sqlalchemy import select
stmt = select(User).where(User.email == email)
```

### XSS

```python
# ❌ خطأ
return f"<div>{user_input}</div>"

# ✅ صحيح
from html import escape
return f"<div>{escape(user_input)}</div>"
```

### CSRF

- استخدام SameSite cookies
- التحقق من Origin header
- Double submit cookies

---

## 🔍 Security Audit

### أدوات الفحص

| الأداة | الاستخدام |
|--------|----------|
| `bandit` | Python security linter |
| `safety` | Dependency vulnerabilities |
| `trivy` | Container scanning |
| `zap` | Web app scanning |

### تشغيل الفحص

```bash
# Python security
bandit -r app/

# Dependencies
safety check -r requirements.txt

# Container
trivy image murassikh/api:latest
```

---

## 📞 الإبلاغ عن ثغرات

إذا اكتشفت ثغرة أمنية، يرجى الإبلاغ عنها عبر:

- **Email**: security@murassikh.com
- **PGP Key**: [رابط]

**لا تكشف عن الثغرة علناً** حتى يتم إصلاحها.

---

**آخر تحديث:** 2026-09-11
