# إعادة تصميم آلية عمل شاشة الدردشة (المساعد الحواري الاستقصائي)

تم تحويل التطبيق من أداة سؤال وجواب خطية (RAG) إلى **مساعد حواري ذكي (Investigative Conversational Agent)** يفهم السياق، يبني ذاكرة داخل الجلسة، ويطرح أسئلة استقصائية تكيفية عند عدم التأكد من حالة المستخدم، قبل تقديم الإرشاد القرآني.

## المهام المنجزة

- [x] **بناء الوكيل الحواري `ConversationalAgent`:**
  - [x] إنشاء الملف `backend/app/services/ai/conversational_agent.py`.
  - [x] هندسة System Prompt مخصص ليكون رفيقاً استقصائياً يتخذ قراراً مستقلاً (State Machine: `Ask` أو `Guide`).
  - [x] دعم تمرير سجل المحادثات (Chat History) وسياق المستخدم (العمر، الجنس، المؤشرات الحيوية، وملامح الوجه).
  - [x] التأكد من أن إرجاع البيانات يتم بتنسيق JSON صارم لضمان استقرار الاستجابة.
  - [x] توفير آلية Fallback محلية في حال توقف الاتصال بواجهة Gemini.

- [x] **تحديث مسار الدردشة الرئيسي (`/api/v1/analyze`):**
  - [x] التعديل في `backend/app/api/v1/endpoints/recommend.py` ليعتمد على سياق الجلسات (Context-Aware).
  - [x] جلب آخر 5 تفاعلات للمستخدم من قاعدة البيانات `HistoryService` لتمثيل الذاكرة القصيرة للوكيل.
  - [x] تمرير السجل إلى الوكيل الحواري الجديد.
  - [x] معالجة حالة الاستقصاء (`action: ask`): إرجاع سؤال الوكيل مباشرة وتخزينه في سجل المستخدم بدون استدعاء RAG.
  - [x] معالجة حالة الإرشاد (`action: guide`): استكمال دورة الـ RAG وتوليد رد مواساة متصل بالسياق مدعم بآية قرآنية.

- [x] **تحديث آلية صياغة الرد المدعم بالآيات:**
  - [x] الاستفادة من رسالة الوكيل المُوَلَّدة (ai_message) لدمجها مع الآية المسترجعة، مما يحسن من الترابط السياقي ويقلل من عدد الطلبات المتكررة لنموذج الذكاء الاصطناعي.

- [x] **التنظيف والتحسين (Cleanup):**
  - [x] إزالة المصنف القديم `EmotionAnalyzer` بالكامل من الشيفرة المصدرية لأنه لم يعد مستخدماً (`backend/app/services/ai/emotion_analyzer.py`).

- [x] **التأكد من سلاسة واجهة المستخدم (Frontend UX):**
  - [x] التأكد من أن واجهة `chat_screen.dart` تدعم بذكاء عرض رسائل الدردشة العادية (الأسئلة الاستقصائية) بدون بطاقة الآية عند إرجاع (`source=None`).

- [x] **اختبارات الاستقرار (Automated Tests):**
  - [x] التأكد من نجاح اختبارات RAG وتوافقها مع التحديثات الجديدة دون الاعتماد على fallbacks تخفي الأخطاء.
  - [x] التأكد من نجاح اختبارات التوثيق وعزل المستخدمين (User Isolation & Auth Tests) مع المسار المحمي الجديد.
